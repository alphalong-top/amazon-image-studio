name: Build Images

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: alphalong-top/deployment-platform/.github/workflows/reusable-build-images.yml@v1
